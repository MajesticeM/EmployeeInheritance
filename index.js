
//Employee Inheritance
function Employee(title) 
{
    function setTitle(titlePlaceholder)
    {
        titlePlaceholder=title;
        return titlePlaceholder;
    };
    
    function getTitle()
    {
        return title;
    }
}

function Engineer(title, isManager) 
{
    function setIsManager(managerPlaceholder)
    {
        managerPlaceholder=isManager;
        return managerPlaceholder;
    };
    
    function getIsManager()
    {
        //Incomplete
        const one=Engineer();
        return 
    }
}
